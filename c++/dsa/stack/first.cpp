#include <bits/stdc++.h>
using namespace std;

int main()
{
  /*
  // creation of stack
  stack<int> s;
  // push of stack
  s.push(2);
  s.push(3);
  // pop of stack
  s.pop();
  cout << "print of top element " << s.top() << endl;
  if (s.empty())
  {
    cout << "stack is empty" << endl;
  }
  else
  {
    cout << "stack is not empty" << endl;
  }
  */

  return 0;
}